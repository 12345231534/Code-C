#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <stdlib.h>

int islower(char c); // ktra chu thuong
int isupper(char c); // ktra chu hoa
int isdigit(char c); // ktra so
int isalpha(char c); // k tra chu cai
char tolower(char c); // bien 1 ki tu thanh ki tu thuong
char toupper(char c); // bien 1 ki tu thanh ki tu hoa
int strlen(char c[]); // ham do dai
int strcmp (char c[], d[]); // ham so sanh 2 chuoi
char* strrev(char c[]); // dao chuoi
char* strlwr(char c[]); // bien 1 chuoi thanh chuio thuong
char* strupr(char c[]); // bien 1 chuoi thanh chuio hoa
char* strcpy(char c[], d[]) // copy noi dung xau d vao xau c
char* strcat(char c[], d[]) // noi xau d vao xau c
 atoi (char c[]) tra ve so int
long long atoll(char c[]) tra ve so long long
int main(){
	
}
